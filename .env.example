# Environment Configuration Template
# 
# INSTRUCTIONS:
# 1. For LOCAL DEVELOPMENT: Copy this file to .env.local
#    cp .env.example .env.local
#    (.env.local takes precedence over .env.development and is ignored by git)
#
# 2. For PRODUCTION: Update .env.production with your actual values
#    (Never put real secrets in committed files - use CI/CD environment variables)
#
# 3. For DEVELOPMENT: .env.development is already configured with defaults

# Application Configuration
VITE_APP_NAME=My React App
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=A modern React starter pack

# API Configuration
VITE_API_BASE_URL=http://localhost:3000/api

# Settings
VITE_ENABLE_DEBUG_MODE=true
# Example: Add your own environment variables here
# VITE_ANALYTICS_ID=your-analytics-id
# VITE_FEATURE_FLAG_NEW_UI=false
# VITE_SENTRY_DSN=your-sentry-dsn

# ENVIRONMENT-SPECIFIC EXAMPLES:
# 
# Development (.env.development or .env.local):
# VITE_API_BASE_URL=http://localhost:3000/api
# VITE_ENABLE_DEBUG_MODE=true
#
# Production (.env.production):
# VITE_API_BASE_URL=https://your-api.example.com/api
# VITE_ENABLE_DEBUG_MODE=false
#
# SECURITY NOTE: All VITE_ variables are PUBLIC (exposed to browser)
# Never put API keys, passwords, or secrets in these files!